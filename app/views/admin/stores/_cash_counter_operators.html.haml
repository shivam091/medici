%div{data: {controller: "nested-forms", nested_forms_wrapper_selector_value: ".cash-counter-form-wrapper"}}
  %template{data: {nested_forms_target: "template"}}
    = form.fields_for :cash_counter_operators, form.object.cash_counter_operators.build, child_index: "NEW_RECORD" do |cash_counter_operator_fields|
      = render_if_exists("admin/stores/cash_counter_operator_form", locals: {form: cash_counter_operator_fields})
  = form.fields_for :cash_counter_operators do |cash_counter_operator_fields|
    = render_if_exists("admin/stores/cash_counter_operator_form", locals: {form: cash_counter_operator_fields})
  %div{data: {nested_forms_target: "target"}}
  %hr
  = button_tag type: :button, class: "btn btn-sm btn-secondary mb-3", data: {action: "click->nested-forms#addAssociation"} do
    = external_svg_tag("svgs/plus.svg")
    = button_text("add_operator")
