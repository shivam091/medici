%div{data: {controller: "nested-forms", nested_forms_wrapper_selector_value: ".medicine-ingredient-form-wrapper"}}
  %template{data: {nested_forms_target: "template"}}
    = form.fields_for :medicine_ingredients, form.object.medicine_ingredients.build, child_index: "NEW_RECORD" do |medicine_ingredient_fields|
      = render_if_exists("medicines/medicine_ingredient_form", locals: {form: medicine_ingredient_fields})
  = form.fields_for :medicine_ingredients do |medicine_ingredient_fields|
    = render_if_exists("medicines/medicine_ingredient_form", locals: {form: medicine_ingredient_fields})
  %div{data: {nested_forms_target: "target"}}
  %hr
  = button_tag type: :button, class: "btn btn-sm btn-secondary mb-3", data: {action: "click->nested-forms#addAssociation"} do
    = external_svg_tag("svgs/plus.svg")
    = button_text("add_ingredient")
